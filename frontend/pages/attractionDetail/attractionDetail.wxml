<view class="container">
  <view class="attraction-name">
    {{attractionName}}
  </view>

  <view class="swiper-wrap">
    <t-swiper
      wx:if="{{imgSrcs.length > 0}}"
      current="{{current}}"
      autoplay="{{autoplay}}"
      duration="{{duration}}"
      interval="{{interval}}"
      navigation="{{navigation}}"    
      imageProps="{{swiperImageProps}}"
      list="{{imgSrcs}}"                 
    />
  </view>

  <view class="description-box">
    <text class="description-text">
      {{description}}
    </text>
  </view>

  <view class="ticket-info">
    <text class="opening-hours">Opening Hours: {{openingHours}}</text>
    <navigator class="reservation-link" url="{{reservationLink}}" open-type="navigate">
      Reserve Tickets
    </navigator>
  </view>

</view>